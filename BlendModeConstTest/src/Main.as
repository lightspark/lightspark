package
{
	import flash.display.Sprite;
	import flash.events.Event;
	import flash.display.BlendMode;
	
	/**
	 * ...
	 * @author Huw Pritchard
	 */
	public class Main extends Sprite 
	{
		
		public function Main() 
		{
			if (BlendMode.ADD == "add")
			{
				trace("Passed");
			}
			else
			{
				trace("Failed");
			}

			if (BlendMode.ALPHA == "alpha")
			{
				trace("Passed");
			}
			else
			{
				trace("Failed");
			}

			if (BlendMode.DARKEN == "darken")
			{
				trace("Passed");
			}
			else
			{
				trace("Failed");
			}

			if (BlendMode.DIFFERENCE == "difference")
			{
				trace("Passed");
			}
			else
			{
				trace("Failed");
			}

			if (BlendMode.ERASE == "erase")
			{
				trace("Passed");
			}
			else
			{
				trace("Failed");
			}

			if (BlendMode.HARDLIGHT == "hardlight")
			{
				trace("Passed");
			}
			else
			{
				trace("Failed");
			}

			if (BlendMode.INVERT == "invert")
			{
				trace("Passed");
			}
			else
			{
				trace("Failed");
			}

			if (BlendMode.LAYER == "layer")
			{
				trace("Passed");
			}
			else
			{
				trace("Failed");
			}

			if (BlendMode.LIGHTEN == "lighten")
			{
				trace("Passed");
			}
			else
			{
				trace("Failed");
			}

			if (BlendMode.MULTIPLY == "multiply")
			{
				trace("Passed");
			}
			else
			{
				trace("Failed");
			}

			if (BlendMode.NORMAL == "normal")
			{
				trace("Passed");
			}
			else
			{
				trace("Failed");
			}

			if (BlendMode.OVERLAY == "overlay")
			{
				trace("Passed");
			}
			else
			{
				trace("Failed");
			}

			if (BlendMode.SCREEN == "screen")
			{
				trace("Passed");
			}
			else
			{
				trace("Failed");
			}

			if (BlendMode.SHADER == "shader")
			{
				trace("Passed");
			}
			else
			{
				trace("Failed");
			}

			if (BlendMode.SUBTRACT  == "subtract")
			{
				trace("Passed");
			}
			else
			{
				trace("Failed");
			}
		}
		
	}
	
}